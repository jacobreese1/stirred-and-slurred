import React from "react";
import { Typography, Box } from "@mui/material";

const Homepage = () => {
  return <Typography variant="h2">Welcome to Stirred and Slurred</Typography>;
};

export default Homepage;
