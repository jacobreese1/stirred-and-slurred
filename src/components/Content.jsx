import { Box, Typography } from "@mui/material";
import React from "react";

const Content = () => {
  return <Typography sx={{ p: "1.6rem " }}>This is initial content</Typography>;
};

export default Content;
